import React from 'react';
import displayForm from './displayForm';

const Settings: React.FC = () => {
  //const clipPath = displayForm.pointsConverted ? `polygon(${displayForm.pointsConverted.join('px,')}px)` : 'none';
  return (
     // <img src="./k_means8_output_main.jpg" alt="Desa Plot" style={{ clipPath }}  ></img>
     <div>Blah</div>
  )
};

export default Settings;
